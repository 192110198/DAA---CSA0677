#include <stdio.h>
unsigned int factorial(unsigned int n,int count)

{
count++;
{
    if (n == 0)
        return 1;
    return n * factorial(n - 1);
}
count++;
count++;
int main()
{
    int num = 5;
    count++;
    printf("Factorial of %d is %d", num, factorial(num));
    return 0;
}
